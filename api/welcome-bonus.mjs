import {adminClient,json} from "./_lib/supabase.mjs";import {requireUser} from "./_lib/auth.mjs";
export default async function handler(req,res){if(req.method!=="POST")return json(res,405,{error:"METHOD_NOT_ALLOWED"});const user=await requireUser(req,res);if(!user)return;try{const db=adminClient();const {data,error}=await db.rpc("claim_welcome_bonus",{p_user_id:user.id});if(error)throw error;if(data?.ok===false)return json(res,409,{error:data.error,message:"Welcome bonus was already claimed."});return json(res,200,{message:"Welcome bonus of 80 H/s granted.",...data})}catch(e){return json(res,500,{error:"WELCOME_BONUS_FAILED"})}}
